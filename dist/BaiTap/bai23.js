"use strict";
class CashPayment {
    pay(amount) {
        console.log(`Paid ${amount} by cash `);
    }
}
class CardPayment {
    pay(amount) {
        console.log(`Paid ${amount} by card `);
    }
}
